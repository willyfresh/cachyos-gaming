[preferred]
default=gnome;gtk;
# GNOME's file chooser is Nautilus and only starts once niri is a
# graphical session. Until then (and whenever that backend fails) the
# click in Chrome does nothing. GTK draws the dialog itself.
org.freedesktop.impl.portal.FileChooser=gtk;
org.freedesktop.impl.portal.Access=gtk;
org.freedesktop.impl.portal.Notification=gtk;
org.freedesktop.impl.portal.Secret=gnome-keyring;
# Screencast stays on the GNOME portal. niri does not implement it.
org.freedesktop.impl.portal.ScreenCast=gnome;
org.freedesktop.impl.portal.Screenshot=gnome;
org.freedesktop.impl.portal.RemoteDesktop=gnome;

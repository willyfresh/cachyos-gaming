# Scratch inbox. Dump obstacles here, then hand the file over.
# Absorbed items live in records/next.md.
super ctrl alt mousescrolling to change window size by 10%
need to work in window sizing with the super-arrow system
the mouse no longer moves windows. i liked that, just wanted a little delay before it triggers.

omarchy webapps: stock omarchy-launch-webapp is chrome --app=URL in the default profile. title-less, but chrome dumps that site's data when the window closes (that's the logged-out messages/bga problem). real PWAs (--app-id) keep the login and draw a CSD title bar + custom tab bar; niri cannot strip those (why borg has the webkit bga wrapper). delorean fix: dedicated --user-data-dir (~/.local/share/delorean/chrome-webapps), --profile-directory=Default, --app=URL, never --app-id, never attach to the running super+b chrome (no uwsm-app). links out of --app windows open in that dir's Default, so that Default has to be the signed-in profile (collapse any named profile into Default). scripts: ~/Projects/delorean/scripts/launch-webapp.sh, launch-or-focus-webapp.sh, collapse-webapp-profile.sh. drop that onto borg; don't wire it from the thinkpad.

super +/- on omarchy is not current-window wider/thinner. it's hyprland window.resize on the left edge: super+minus = expand left (x=-100), super+equal = shrink left (x=+100). shift is top/bottom, alt ±25px, ctrl ±300px. minus makes it wider, plus makes it thinner. don't copy that. want: this window/column wider / thinner. borg already has niri set-column-width ±10% (of the screen). keep that; if it still feels wrong it's the step or a maximized/preset column, not the action.

I love what omarchy does to websites when I press super ctrl f. let's make that happen in niri.

every day my niri and my delorean keybinds inch closer to homogeny. let's examine the progress and decide what the next step should be.
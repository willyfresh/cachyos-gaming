# Extra packages from CachyOS/Arch repos, on top of a stock niri install.
# One package per line. Blank lines and comments are ignored.
#
# Prefer:  ./scripts/pkg-add.sh steam lutris
# That installs, records here (or in aur.txt), and logs records/journal.md

# AUR helper used by setup.sh / pkg-add.sh for packages/aur.txt
paru

# Super tap -> launcher (see configs/keyd/default.conf)
keyd

# Gaming: CachyOS meta pulls proton-cachyos-slr, umu-launcher, wine-cachyos-opt,
# winetricks, protontricks, vulkan-tools, and the 32-bit libs Steam needs.
cachyos-gaming-meta
steam
heroic-games-launcher-bin
mangohud
lib32-mangohud
gamescope
goverlay
